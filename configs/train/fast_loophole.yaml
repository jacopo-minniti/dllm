training:
  core:
    model: fast-dllm
    mode: loophole_bptt
    backbone: lora
    epochs: small
    bptt: 2
  
  hyperparameters:
    learning_rate: 2e-05
    per_device_train_batch_size: 8
    gradient_accumulation_steps: 4
  
  dataset:
    dataset: tulu