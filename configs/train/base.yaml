training:
  core:
    model: fast-dllm
    mode: loophole_bptt
    backbone: unfrozen
    epochs: small
  
  hyperparameters:
    learning_rate: 2e-05
    per_device_train_batch_size: 1
    gradient_accumulation_steps: 4
  
  dataset:
    dataset: tulu
