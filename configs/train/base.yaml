training:
  core:
    model: fast-dllm
    mode: base
    backbone: trainable
    epochs: small
  
  hyperparameters:
    learning_rate: 2e-05
    per_device_train_batch_size: 8
    gradient_accumulation_steps: 4
  
  dataset:
    dataset: tulu
